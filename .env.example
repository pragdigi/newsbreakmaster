FLASK_SECRET_KEY=change-me-in-production
# Optional: default NewsBreak organization IDs (comma-separated) if not entered at login
NEWSBREAK_DEFAULT_ORG_IDS=
# Optional: protect the whole app with HTTP Basic Auth (browser login before NewsBreak token screen)
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=
